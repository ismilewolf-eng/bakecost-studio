# 烘焙与餐饮配方成本核算器 —— 实战方案计划书

**产品定位**：Bakery Recipe Costing Calculator（纯前端 PWA，零后端，本地隐私存储，一键生成可打印/可导出的配方成本单）
**目标用户**：海外独立烘焙师、Home Bakery、小型 Café/Pizzeria 主理人、烹饪课程内容创作者
**核心卖点**：Excel 太复杂，同类站点太老太丑，我们用手机就能在厨房台面上边称重边算价

---

## 模块一：核心数学模型与计算公式

这是产品的信任基础，必须公式透明、可追溯、可导出，让用户能验证"为什么这个价格是对的"。

### 1.1 原料单位换算体系

**三层换算模型**：

```
第一层：采购单位 → 基准单位（标准化存储）
第二层：基准单位 → 配方用量单位（食谱书写习惯）
第三层：容重换算（体积 ↔ 重量，靠密度系数表）
```

**基准单位选择**：所有原料内部统一折算为「克 (g)」和「毫升 (ml)」两个基准单位存储，避免多单位链式误差累积。

**换算公式**：

```
单位成本(基准) = 采购总价 ÷ 采购包装规格(折算为基准单位)

例：一袋面粉 $12.99 / 5 lb
   5 lb = 5 × 453.592 g = 2267.96 g
   单位成本 = $12.99 ÷ 2267.96 g = $0.005727 /g
```

**内置换算系数表（可编辑、可扩展）**：

| 类型 | 单位 | 换算为克(g)系数 |
|---|---|---|
| 重量 | kg / lb / oz | 1000 / 453.592 / 28.3495 |
| 体积→重量 | tsp / tbsp / cup / ml | 依赖「密度系数 ρ (g/ml)」，如：水=1.0，面粉=0.53，糖=0.85，黄油=0.95，蜂蜜=1.42 |

体积转重量公式：
```
重量(g) = 体积(ml) × 密度系数ρ(g/ml)
1 cup ≈ 236.59 ml → 重量 = 236.59 × ρ
1 tbsp ≈ 14.79 ml,  1 tsp ≈ 4.93 ml
```

> **产品要点**：内置一份"常用烘焙原料密度库"（面粉/糖/盐/黄油/牛奶/可可粉/坚果碎等 40+ 项），用户选原料名即自动带出密度，避免手动查询——这是相对 Excel 模板最大的隐性效率优势。

### 1.2 出成率 / 损耗率（Yield %）

用于处理"面团醒发损耗""水果去皮去核""烤后缩水""切割边角料"等场景。

```
出成率 Yield% = 可用成品重量 ÷ 投入原料总重量 × 100%

损耗调整后实际用量 = 配方标注用量 ÷ Yield%

例：草莓去蒂去烂后出成率 85%
   配方需要净草莓 500g
   实际需采购 = 500 ÷ 0.85 = 588.2g
   该原料实际分摊成本 = 588.2g × 单位成本
```

对于烘焙场景的"烤损率"（Baking Loss %，面团烤制后水分蒸发导致的重量损失）：

```
烤损率 = (烤前面团重 − 烤后成品重) ÷ 烤前面团重 × 100%
用于反推：需要准备的面团总重 = 目标成品总重 ÷ (1 − 烤损率)
```

再叠加「份数拆分」：
```
单份原料成本 = 该配方原料总成本(已计入yield) ÷ 总产出份数
```

### 1.3 包装耗材与固定开销分摊（Packaging & Overhead）

三段式成本结构，这是很多免费工具漏掉、但用户最终定价最关心的部分：

```
总成本 = 原料成本(含损耗) + 包装成本 + 人工成本 + 分摊固定开销

① 原料成本（见1.1+1.2）

② 包装成本 = Σ(单件包材单价)
   如：蛋糕盒 $0.8 + 手提袋 $0.3 + 标签 $0.1 = $1.2/份

③ 人工成本 = (制作总耗时 ÷ 60) × 时薪
   例：耗时90分钟，时薪$25 → 人工成本 = 1.5 × $25 = $37.5（整批）
   → 单件人工成本 = 37.5 ÷ 产出份数

④ 固定开销分摊(Overhead Allocation) 有两种模式供用户切换：
   模式A - 按比例法：Overhead = 原料成本 × 开销系数(如15%~30%，行业经验值)
   模式B - 按批次分摊法：
      单件开销 = (月度固定成本 ÷ 月度总产出份数)
      月度固定成本 = 房租+水电+设备折旧+保险 等
```

### 1.4 建议售价与目标利润率反推（Food Cost % ⇄ Markup）

这是餐饮定价的行业黄金公式，必须双向可反推：

**正向（已知成本求售价）**：
```
Food Cost % = 食材成本 ÷ 售价 × 100%
（餐饮行业健康区间通常 28%~35%，烘焙精品店可放宽到 20%~30%）

建议售价 = 单件总成本 ÷ 目标Food Cost%

例：单份总成本 $3.20，目标Food Cost% = 30%
   建议售价 = 3.20 ÷ 0.30 = $10.67
```

**Markup 乘数法（等价换算）**：
```
Markup倍数 = 1 ÷ Food Cost%
Food Cost% = 30% → Markup = 3.33x
建议售价 = 单件总成本 × Markup倍数
```

**反向验算（已知期望售价求实际利润率）**：
```
实际毛利率 GP% = (售价 − 单件总成本) ÷ 售价 × 100%
实际净利润(每件) = 售价 − 单件总成本
盈亏平衡产量 = 固定成本合计 ÷ (单件售价 − 单件变动成本)
```

> 产品交互建议：三个数字用**双向拖动滑块联动**——拖"目标Food Cost%"售价自动变，拖"售价"利润率自动变，拖"利润率"两者同步，这种即时反馈是纯前端单页应用的天然优势，Excel 做不到这种丝滑体验。

**计算引擎汇总结构（用于代码实现的数据流）**：
```
原料表[i] = {采购价, 采购规格, 密度, 用量, 用量单位, 损耗率}
  → 单原料成本[i] = f(1.1) × f(1.2)
原料总成本 = Σ单原料成本[i]
批次总成本 = 原料总成本 + 包装成本 + 人工成本 + 分摊开销
单件成本 = 批次总成本 ÷ 产出份数
建议售价 = 单件成本 ÷ 目标FoodCost%
```

---

## 模块二：产品体验与降维打击点

对标现状：老牌工具（如部分 chefs-resources 类站点）UI 陈旧、表单式录入、桌面导向；Excel 模板功能强但门槛高（公式易崩、无移动端、隐私顾虑上传云端）。降维打击的核心策略是**"厨房可用性"**：真实场景是用户一手端着秤、一手拿手机，站在灶台边而非坐在电脑前。

### 2.1 卡片式 UI —— 拒绝表格恐惧症

- 原料录入用**独立卡片**而非 Excel 式行列，每张卡片= 1 个原料，大字号、图标化单位选择器（磅秤图标=重量，量杯图标=体积）
- 卡片支持左滑删除、长按拖拽排序（模仿 Todoist/Notion 的移动端手感）
- 成本结果用**仪表盘式大字卡**呈现：单件成本、建议售价、利润率三个数字用色块（红/黄/绿）实时高亮，超出健康 Food Cost% 区间时卡片变红提醒

### 2.2 移动端厨房触控交互

- **大触控目标**：所有数字输入框≥48px 高度，支持"+/-"步进按钮而非纯键盘输入（沾了面粉的手指戳大按钮比打字更可行）
- **语音输入**（可选增强）：用 Web Speech API 让用户口述"两杯面粉"，自动解析为 480g（配合密度库）——这是对着屏幕打字之外的"解放双手"体验
- **免登录、免加载动画**：PWA 安装后图标常驻主屏，离线打开秒开，不会出现传统网站的"loading spinner + 登录墙"
- **一键生成成本单**：结果页支持直接导出为图片/PDF（html2canvas + jsPDF 纯前端方案），带品牌 Logo 水印区，方便用户发给合伙人或用于成本存档

### 2.3 100% 本地隐私安全（核心信任杠杆）

竞品普遍要求注册账号、上传配方到云端服务器——对独立烘焙师而言，**配方本身是商业机密**（利润率、独家配方比例）。

- 全部数据存储在浏览器 `localStorage`/`IndexedDB`，无需注册，无网络请求上传敏感数据
- 首页明确文案标注："🔒 你的配方永远只存在你的设备上，我们看不到，也传不走"——这是强有力的差异化信任信号，直接打消目标用户对"配方外泄给竞争对手"的顾虑
- 支持"导出JSON备份 / 导入恢复"，让用户自己掌控数据迁移，而不依赖云同步

### 2.4 对比传统 Excel 模板的降维点总结

| 维度 | 传统 Excel 模板 | 本产品 |
|---|---|---|
| 上手成本 | 需要懂公式、常被用户改坏 | 零学习成本，填空即得结果 |
| 单位换算 | 手动查表易出错 | 内置密度库自动换算 |
| 移动端 | 几乎不可用 | 专为厨房触控设计的 PWA |
| 数据安全 | 云盘/邮件传输，易泄露 | 纯本地存储 |
| 结果呈现 | 静态表格 | 实时联动仪表盘+一键导出 |
| 多配方管理 | 多个文件混乱 | 卡片列表统一管理，可复制克隆配方 |

---

## 模块三：SEO 长尾词矩阵与内容截流策略

策略核心：不与"cost calculator"这类头部词硬拼，而是按**细分品类 × 搜索意图**做长尾矩阵，用工具页+配套说明内容页组合截流。

### 3.1 搜索意图分层

1. **工具型意图**（Transactional）：直接想用计算器 → 落地页 = 工具本身，标题必须含"calculator/free/online"
2. **知识型意图**（Informational）：想理解怎么算 → 需配套的"How to"内容页，用来吃到更大流量池并内链导向工具
3. **模板型意图**（Navigational-ish）：想找现成模板替代品 → 主打"vs Excel template"对比词

### 3.2 分场景长尾词矩阵

**蛋糕（Cake）场景**：
- cake cost calculator free
- how to price a cake for sale (by size / by slice)
- wedding cake pricing calculator
- cake decorating cost breakdown
- how much to charge for a custom birthday cake
- cake batter yield calculator (pan size conversion)

**曲奇/饼干（Cookies）场景**：
- cookie cost per dozen calculator
- how to price cookies for a bake sale
- sugar cookie recipe cost breakdown
- cottage bakery pricing guide cookies
- cookie packaging cost calculator

**比萨/餐饮（Pizza & F&B）场景**：
- pizza dough cost per pizza calculator
- restaurant food cost percentage calculator
- menu pricing calculator free online
- ideal food cost percentage by dish type
- pizza topping cost breakdown template

**通用烘焙创业者场景（承接私域/长尾流量）**：
- home bakery pricing calculator free
- cottage food pricing spreadsheet alternative
- bakery profit margin calculator
- recipe costing app no login
- baking ingredient unit converter g to cups
- free food cost calculator no excel needed

### 3.3 内容截流打法

- **工具页矩阵**：为高转化词各建一个轻量落地变体（如 `/cake-cost-calculator`、`/pizza-cost-calculator`、`/cookie-pricing-calculator`），复用同一计算引擎但预填该品类的原料模板与默认损耗率，提高长尾词的页面相关性与转化率
- **内容页矩阵**：围绕"How to price X"写 8-12 篇教程文章，每篇文末嵌入对应工具的 CTA 按钮，形成"内容引流 → 工具留存 → PWA 安装"漏斗
- **结构化数据**：为每个工具页添加 `SoftwareApplication` + `FAQPage` Schema.org 标记，提升在 Google 搜索结果中获得富文本片段（Rich Snippet）的概率
- **反向链接策略**：向 Reddit r/AskBaking、r/KitchenConfidential、烘焙类 Facebook Group 分享免费工具（非硬广，以"我做了个小工具希望有帮助"口吻），获取自然外链与早期种子用户反馈

---

## 模块四：商业化变现模式

单页轻量工具的变现要克制，不能因广告/弹窗破坏"厨房中一键计算"的核心体验，采用**分层非侵入式变现**。

### 4.1 展示广告（基础流量变现）

- 使用 Google AdSense，仅在**结果导出页下方**和**非核心浏览区域**投放，禁止在录入表单区插入广告（避免打断计算流程）
- PWA 离线模式下不加载广告（技术上也做不到），保证核心离线体验纯净

### 4.2 联盟返佣（Affiliate，符合工具属性的高转化位置）

在结果页与"原料密度库"页面自然嵌入相关推荐，例如：
- 亚马逊联盟：推荐用户常缺的厨房秤、烘焙温度计、包装耗材（蛋糕盒/手提袋）——在用户刚计算完"包装成本"卡片旁边推荐对应商品，转化路径极短
- 烘焙原料批发平台联盟（如 WebstaurantStore、Restaurant Depot 等 B2B 渠道的联盟计划）
- SaaS 类联盟：向想进一步做订单管理/收款的用户推荐 Square、Shopify 等餐饮收单工具，赚取注册返佣

### 4.3 进阶定制模板（一次性付费，非订阅优先测试）

- **免费版**：基础计算器功能全开放（保持"无门槛试用"获客）
- **Pro 模板包**（$4.9-$9.9 一次性）：
  - 品牌化 PDF 导出模板（可上传自己 Logo、自定义配色的成本单/报价单）
  - 行业预设配方库（60+ 常见烘焙/餐饮标准配方及行业平均损耗率，省去用户自己查资料）
  - 批量配方管理 + Excel/CSV 导出（给已有一定规模、需要交给会计的用户）
- 定价策略参考"一次性小额付费"而非订阅制，更契合独立小微烘焙业主的付费心理，减少订阅疲劳带来的流失

### 4.4 变现优先级路线

```
第一阶段（0-3个月）：AdSense + 亚马逊联盟（成本最低，快速验证流量）
第二阶段（有稳定日活后）：上线 Pro 模板一次性付费
第三阶段（若形成社群）：探索 B2B 授权（烘焙培训机构/中小连锁店批量授权定制版）
```

---

## 模块五：3天极速交付落地排期

目标：Day1 出可用原型，Day2 完成 PWA+SEO 基建，Day3 部署上线并提交索引，全程单人可执行。

### Day 1：核心产品原型（单文件 HTML + Vue3 + Tailwind CDN）

| 时间 | 任务 |
|---|---|
| 上午 | 技术选型落地：Vue3(CDN, `<script type="module">`) + Tailwind CDN + 单 HTML 文件起步，无需构建工具，方便当天就能跑；搭建原料卡片录入组件、单位换算引擎(模块一 1.1/1.2 公式落地为 JS 纯函数) |
| 下午 | 成本计算引擎：包装/人工/开销分摊 + 建议售价反推(1.3/1.4)，接入 localStorage 做配方增删改查持久化 |
| 晚上 | UI 打磨：卡片式录入、结果仪表盘、滑块联动交互(模块二)；移动端响应式自测(Chrome DevTools 设备模拟) |

**产出**：可在手机浏览器本地打开、完整跑通"填原料→出成本→出建议售价"的可用 Demo

### Day 2：PWA 离线化 + 导出 + SEO 基建

| 时间 | 任务 |
|---|---|
| 上午 | 拆分/整理为可维护结构（若仍单文件也需模块化 JS 变量组织）；接入 `manifest.json` + Service Worker（Workbox CDN 或手写简单 cache-first 策略），实现离线可用与"添加到主屏幕" |
| 下午 | 一键导出功能：html2canvas 截图导出成本单图片 + jsPDF 导出 PDF；密度换算库数据补全（40+常用原料） |
| 晚上 | SEO 基建：为首页与 2-3 个品类变体页（蛋糕/曲奇/披萨，模块三）写好 Title/Meta/H1，插入 `SoftwareApplication`+`FAQPage` 结构化数据，生成 `sitemap.xml`、`robots.txt` |

**产出**：可安装的离线 PWA + 完成基础 SEO 元数据的多落地页结构

### Day 3：部署上线 + 搜索引擎提交 + 冷启动

| 时间 | 任务 |
|---|---|
| 上午 | Cloudflare Pages 部署：连接 Git 仓库或直接拖拽上传静态文件，绑定自定义域名，开启 HTTPS（自动） |
| 中午 | 全设备回归测试：真机 iOS Safari + Android Chrome 测试 PWA 安装、离线模式、导出功能是否正常；Lighthouse 跑分（重点看 PWA/Performance/SEO 三项，目标 90+） |
| 下午 | Google Search Console 提交：验证站点所有权（DNS 或 HTML 标签方式），提交 `sitemap.xml`，用 URL 检查工具手动请求首页与各品类页的索引 |
| 晚上 | 冷启动分发：接入 AdSense 申请（先挂代码，审核有滞后属正常）；在 Reddit/Facebook 烘焙社群自然分享；将站点提交至 Product Hunt / 相关工具导航站(如 There's An AI For That 类目) |

**产出**：正式上线、已提交索引、启动第一波自然流量导入的可运营产品

---

### 交付里程碑总览

```
D1 → 能用（计算引擎+基础UI跑通）
D2 → 能装（PWA离线+导出+SEO骨架）
D3 → 能被搜到（部署+GSC提交+冷启动分发）
```

后续迭代建议优先级：① 补全品类预设模板（承接SEO长尾页转化）② Pro模板付费墙 ③ 多语言(西语/法语烘焙市场增量)。
